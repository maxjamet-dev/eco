# Recursos empaquetados

En el primer arranque / setup se colocan aquí:
- capture/meetcap.exe (binario Rust)
- python/ (venv + worker.py) si se opta por empaquetar
- whispercpp/ (binario + modelo de fallback CPU)
